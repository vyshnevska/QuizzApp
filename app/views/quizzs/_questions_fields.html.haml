%ul.nested-fields
  %p
    = f.label "Question:"
    = f.text_field :title
    %p
      = f.fields_for :answers do |builder|
        = render :partial => 'answers_fields', :locals => { :f => builder }
.links
  = f.hidden_field :_destroy
  = link_to_remove_fields "Remove Question", f