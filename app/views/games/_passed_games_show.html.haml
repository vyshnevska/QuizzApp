:javascript
  $(function() {
    $(document).ajaxSuccess(function(settings) {
      settings.responseText;
    });
  });

.games.passed
  .section
    %h1.gorgeous Passed games
    %hr.section_caption
    #passed_games
      = render @games_passed
  .footer
    #paginator_passed
      = paginate @games_passed, :params => {:controller => 'games', :action => 'paginate_passed'}, :remote => true

  .section
    %hr