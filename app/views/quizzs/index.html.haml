%h1 Listing quizzs


- @quizzs.each do |quizz|
  %ul
    = quizz.id
    = quizz.description
    - quizz.questions.each do |q|
      %li
        = q.title
        - q.answers.each do |a|
          %ul
            = a.content
  %ul
    %li= link_to 'Show', quizz
    %li= link_to 'Edit', edit_quizz_path(quizz)
    %li= link_to 'Destroy', quizz, :method => :delete, :data => { :confirm => 'Are you sure?' }
%br
= link_to 'New Quizz', new_quizz_path
